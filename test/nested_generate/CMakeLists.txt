# SPDX-License-Identifier: Apache-2.0

macro(define_nested_generate_testbench_vars)
  set(NESTED_GENERATE_RTL_ROOT "${PROJECT_SOURCE_DIR}/test/nested_generate")
  set(NESTED_GENERATE_DESIGN_FILE_LIST "${NESTED_GENERATE_RTL_ROOT}/design.flist")
  set(NESTED_GENERATE_DESIGN_TOP "top")
  set(NESTED_GENERATE_SOURCES
    ${NESTED_GENERATE_RTL_ROOT}/top.sv
  )

  set(RTL_ROOT "${NESTED_GENERATE_RTL_ROOT}")
  set(DESIGN_FILE_LIST "${NESTED_GENERATE_DESIGN_FILE_LIST}")
  set(DESIGN_TOP "${NESTED_GENERATE_DESIGN_TOP}")
  set(SOURCES "${NESTED_GENERATE_SOURCES}")
  set(SYNTH_HDL_FRONTEND "slang")
endmacro()
