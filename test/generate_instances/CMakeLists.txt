# SPDX-License-Identifier: Apache-2.0

macro(define_generate_instances_testbench_vars)
  set(GENERATE_INSTANCES_RTL_ROOT "${PROJECT_SOURCE_DIR}/test/generate_instances")
  set(GENERATE_INSTANCES_DESIGN_FILE_LIST "${GENERATE_INSTANCES_RTL_ROOT}/design.flist")
  set(GENERATE_INSTANCES_DESIGN_TOP "top")
  set(GENERATE_INSTANCES_SOURCES
    ${GENERATE_INSTANCES_RTL_ROOT}/top.sv
  )

  set(RTL_ROOT "${GENERATE_INSTANCES_RTL_ROOT}")
  set(DESIGN_FILE_LIST "${GENERATE_INSTANCES_DESIGN_FILE_LIST}")
  set(DESIGN_TOP "${GENERATE_INSTANCES_DESIGN_TOP}")
  set(SOURCES "${GENERATE_INSTANCES_SOURCES}")
  set(SYNTH_HDL_FRONTEND "slang")
endmacro()
